# GitHub Personal Access Token
# Needs: Actions (read + write) on Northform-qa/northform
GITHUB_TOKEN=

# Per-client passphrase — one var per slug, slug uppercased
# Pattern: PORTAL_PASSPHRASE_<SLUG>
PORTAL_PASSPHRASE_NORTHFORMQA=
